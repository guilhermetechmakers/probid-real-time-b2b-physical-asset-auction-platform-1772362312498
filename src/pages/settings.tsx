/**
 * Settings / Preferences page for ProBid.
 * Account, notifications, subscription, KYC, integrations, API keys, security.
 */
import { SettingsDashboard } from '@/components/settings'

export function SettingsPage() {
  return <SettingsDashboard />
}
